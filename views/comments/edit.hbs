<h1>Edit Comment</h1>

<form class="" action="/comments/{{comment.id}}" method="post">
  <label for='user'>User</label>
  <select class="" name='user' id='user'>
    <option value='{{comment.user.id}}' selected><b>{{comment.user.name}}</b></option>
    {{#each users}}
      <option value='{{this.id}}'>{{this.name}}</option>
    {{/each}}
  </select>

  <label for="content">Text</label>
  <input type="text" name="content" placeholder={{comment.content}} id="content" value={{comment.content}}>

  <br><br>
  <button type="submit">Edit Comment</button>
</form>

<br>
<a href="/comments/{{comment.id}}">Back to Comment Shows</a>
